//- Using example: +link-button({text: 'Button text', linkTo: 'Link directs to', theme: 'primary/secondary'})

mixin link-button(params)
  - var baseClass = 'link-button'
  - var classes = [baseClass]

  case params.theme
    when 'primary'
      - classes.push(baseClass + '--theme--primary')
    when 'secondary'
      - classes.push(baseClass + '--theme--secondary')

  if params.narrow
    - classes.push(baseClass + '--narrow')

  a(class=classes, href=params.linkTo)!= params.text
